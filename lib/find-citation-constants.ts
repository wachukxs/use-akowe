/**
 * Copy and constants for the Find Citation modal (project editor).
 * Centralized so UI strings can be tested and updated in one place.
 */

/** Message shown in tooltip when "Search for new citations" is disabled (no search topic). */
export const SEARCH_TOPIC_REQUIRED_TOOLTIP =
  'Enter a search topic to find citations.';

/** Label for the floating "Find citation" control shown on editor right-click (Option B context menu). */
export const CONTEXT_MENU_FIND_CITATION_LABEL = 'Find citation';
